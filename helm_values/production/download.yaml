branch: production
hpa:
  minReplicas: 3
  maxReplicas: 30
app:
  configToUse: download-production
  name: download
  requests:
    cpu: 0.5
  limits:
    cpu: 1
aws:
  tags:
    service: "download-service"
    env: "production"