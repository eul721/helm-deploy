branch: develop
hpa:
  minReplicas: 1
  maxReplicas: 3
app:
  configToUse: publisher-service-develop
  name: download
  requests:
    cpu: 0.4
  limits:
    cpu: 0.8
aws:
  tags:
    service: "download-service"
    env: "develop"