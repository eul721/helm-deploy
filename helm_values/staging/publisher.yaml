branch: staging
hpa:
  minReplicas: 1
  maxReplicas: 3
app:
  configToUse: publisher-production
  name: publisher
  requests:
    cpu: 0.3
  limits:
    cpu: 0.8
aws:
  tags:
    service: "publisher-service"
    env: "staging"