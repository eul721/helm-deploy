import { ContentfulPrerequisite } from '../contentful/contentfulprerequisite';

/**
 * HTTP model representing a single prerequisite of a game
 * Currently overlaps completely with its contentful equivalent but this may change
 */
export type Prerequisite = ContentfulPrerequisite;
